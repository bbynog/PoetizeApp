import { StyleSheet } from 'react-native';

export const styles = StyleSheet.create({
  container: {
    flex: 1,
    justifyContent: 'center',
    alignItems: 'center',
  },
  gradient: {
    borderRadius: 8,
    height: 40,
    width: 150,
    alignItems: 'center',
    justifyContent: 'center',
    elevation: 5,
  },
});
